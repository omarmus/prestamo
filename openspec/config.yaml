# OpenSpec Configuration — prestamos-app
# Auto-generated by sdd-init

schema: spec-driven

context: |
  Fintech de préstamos digitales para Bolivia. Greenfield project.

  Frontend: Next.js + TypeScript + TailwindCSS + Shadcn UI + TanStack Query + React Hook Form + Zod
  Backend:  NestJS + TypeScript + Prisma ORM + BullMQ + Passport/JWT + OpenAPI
  Database: PostgreSQL + Redis + S3
  AI:       OpenAI + Claude + Gemini + DeepSeek + OCR + Embeddings
  DevOps:   Docker + GitHub Actions + Terraform (future) + AWS
  Monitor:  Grafana + Prometheus + Loki + CloudWatch
  Architecture: DDD + Clean Architecture + Modular Monolith (event-driven)

  Phase 0: Basic Scaffold + CI + AWS infra (ECS Fargate, RDS, ElastiCache)
  Phase 1: Auth Module (NestJS) + Client Onboarding (Next.js)
  Phase 2: Loan Application, Evaluation, Credit Risk
  Phase 3: Disbursement, Collections, Payments
  Phase 4: AI scoring, WhatsApp integration, Admin dashboard

testing:
  strict_tdd: false
  runner: none
  reason: "Greenfield project — no test runner, no package.json, no source code yet. Install Jest or Vitest after init."

rules:
  proposal:
    - Justify every dependency added
    - Include risk assessment for financial operations
    - Include rollback plan for schema migrations
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Cover error states for every flow (financial domain)
  design:
    - Include sequence diagrams for complex flows
    - Document ADRs with rationale
    - Always include data model changes
  tasks:
    - Group by module, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
